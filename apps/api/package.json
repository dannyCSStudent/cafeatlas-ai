{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload",
    "build": "python3 -m compileall app tests",
    "lint": "python3 -m compileall app tests",
    "check-types": "python3 -m compileall app tests",
    "test": "python3 -m pytest",
    "seed": "python3 seed.py"
  }
}
